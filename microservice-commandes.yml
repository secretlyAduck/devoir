mes-config-ms:
  commandes-last: 10  # Custom property for recent commands
server:
  port: 8080  # Application port
management:
  endpoints:
    web:
      exposure:
        include: refresh, health, env
